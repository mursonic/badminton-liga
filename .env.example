# Pflichtfeld: Geheimschlüssel für JWT-Session-Tokens
# Generieren mit: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=dein-geheimer-schluessel-mindestens-32-zeichen

# Pfad zur SQLite-Datenbankdatei (Standard: ./data/liga.db)
DATABASE_URL=file:./data/liga.db
